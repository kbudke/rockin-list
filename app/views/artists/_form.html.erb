<body class="bioRhyme">
<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= form_for(@artist) do |f| %>
      <% if @artist.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

          <ul>
          <% @artist.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

  <div class="form-group">
    <%= f.text_field :name, placeholder: "Artist/Group Name", class: "form-control" %>
  </div>

  <div class="form-group">
    <strong>Aritst/Group Image:</strong>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit "Let's Jam!", class: "btn btn-success" %>
  </div>
<% end %>
  </div>
</div>
</body>